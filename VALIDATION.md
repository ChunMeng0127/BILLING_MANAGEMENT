# Validation — 9 September 2026

## Completed locally

- .NET 10 Release build: **0 errors, 0 warnings**.
- Automated tests: **39 passed, 0 failed, 0 skipped** in the local PostgreSQL run. This includes master-data edits for all five master types, active/inactive changes, historical snapshot preservation, future-only schedule edits, stale engagement/schedule-version rejection, schedule-validated generation modes including forged/missing-mode rejection, recurring manual replacement advancement and anchor realignment, One-Off completion, Ad-Hoc manual periods, overlap/date rejection and staff-only authorization, the allocation-form regression covering blank unused invoice, receipt and worker-payment rows, billing/invoice correction coverage including membership replacement, same-total concurrency and flow-relevant consolidated-party validation, Part 3 receipt metadata, assignment entitlement and payment metadata corrections with stale-version and active-payment locks, Part 4 audit-preservation, derived-status locks and edit-route authorization coverage, separate customer billing/revenue-share base snapshots with migration backfill and dependency-locked correction, Part 4.6 receipt allocation and historical BillingRecord amount corrections, consolidated-invoice receipt attribution proportional to InvoiceLine / Invoice.Total, Part 4.7 active 0% worker assignments, retained LCM gross, payment rejection, correction and dependency-lock coverage, and Part 5 weekly progress uniqueness, Monday–Sunday validation, future/past submission rules, stale-version handling, cancelled-assignment rejection, worker/manager/firm scope and worker WorkItem-edit rejection.
- JavaScript tests: **9 passed, 0 failed**. They cover per-flow eligibility, partial allocations, input maximums, clearing on Customer → Manager and Manager → LCM flow changes (including rows that remain eligible), preserved values on initial load and ordinary grid refreshes, eligible row counts, the all-allocated empty state, external initialization under the production Content Security Policy and receipt-total calculation from corrected allocations.
- Applied all committed EF migrations through `20260909094200_WeeklyWorkerProgressReporting` to a fresh PostgreSQL database and seeded the initial Identity roles/admin and optional sample master records.
- EF migration verification: `20260909094200_WeeklyWorkerProgressReporting` is a new additive migration creating only the assignment-linked weekly progress report table, its validation checks and the unique assignment/week index; after applying it, the model has no pending changes.
- Verified allocation percentages, rounding, four worker-share rates, duplicate and overlapping service-period rejection, invoice splits/consolidation/partial allocation and all three flow caps, partial/multiple-assignment payments, historical snapshots after editing engagement percentages, cancellation rules and wrong-worker rejection.
- Verified same-customer customer-invoice consolidation is allowed while mixed customers are rejected; Manager and LCM flows retain their permitted consolidation. Verified invoice numbers can be reused by different issuers and are rejected for the same issuer.
- Verified customer, service, accounting firm and manager changes are rejected after billing exists, including forged POST values. Billing amount, percentages, valid schedule settings, end date, status and notes remain editable, and historical access stays with the original firm and manager.
- Verified Admin/InternalUser master edits update current directory data without recalculating BillingRecord, revenue-share or WorkerAssignment snapshots. External roles are read-only for master and schedule edit routes.
- Verified the dedicated Edit schedule screen updates future frequency, next period start and anchor day with engagement/schedule version checks; existing billing records remain unchanged, future generation follows the new settings, and overlapping or out-of-date settings are rejected.
- Verified an Engagement form carrying an older ScheduleVersion cannot overwrite a separately edited schedule, while the normal Engagement.Version concurrency check remains active. Recurring manual replacements require the current scheduled start, accept shorter or longer valid periods, move NextPeriodStart to manual PeriodEnd + 1 day and realign AnchorDay; One-Off replacements clear the next start, Ad-Hoc periods remain arbitrary and non-recurring, active overlap and engagement EndDate limits remain enforced, and cancellation does not rewind the schedule.
- Verified BillingController and BillingService reject missing/forged old mode flags and invalid frequency/mode combinations inside the serializable transaction; recurring Scheduled and Replacement modes still enforce the current NextPeriodStart, One-Off cannot be detached, and Ad-Hoc remains the only arbitrary manual mode.
- Verified staff billing corrections enforce engagement-date bounds, active-period overlap protection, billing/invoice/assignment/payment locks and billing/work-item version checks. Customer billing amount corrections are allowed with active customer invoice allocations when no affected invoice has an active receipt and the corrected amount covers those allocations; revenue-share base corrections remain blocked by active manager/LCM invoices, worker assignments or worker payments. Both correction paths preserve CreatedAt/CreatedBy, recalculate derived billing state and reject stale versions. Verified staff invoice corrections allow issuer-scoped number/date edits, preserve immutable flow and header parties, add/remove/replace lines before receipts, force a header version advance for same-total redistributions, recalculate totals and derived states for removed and added records, allow metadata-only edits after receipts, and clearly reject allocation changes while active receipt allocations exist. External-role edit routes remain inaccessible.
- Verified customer receipt date/reference and existing allocation amount corrections use serializable version checks, preserve CreatedAt/CreatedBy/RequestId and allocation row identity, recalculate receipt total plus all affected Invoice/BillingRecord payment statuses, exclude the current receipt's old allocation from outstanding-balance checks, and reject added/removed membership, zero/negative/over-precision, stale, cancelled and overpayment edits. Changing membership still uses cancellation and replacement. Verified worker assignment worker/percentage corrections are staff-only, use the immutable LCM gross snapshot, allow explicit 0% assignments with RM0.00 entitlement, retain active operational visibility, reject positive payments with no unpaid entitlement, reject active payment edits and combined percentage/rounded-entitlement overages, and recalculate entitlement before payment. Verified worker payment date/reference corrections use serializable version checks; payment allocation corrections remain cancel-and-re-enter to preserve auditability and unpaid-entitlement caps.
- Verified invoice header validation is flow-specific: customer invoices require one firm and customer; manager invoices require one manager and firm while allowing multiple underlying customers; LCM invoices require one manager while allowing multiple firms and customers. Header relationships remain immutable.
- Verified New Invoice displays separate customer, Manager and LCM caps, active allocated amounts and remaining balances and updates them when the selected flow changes. Allocation inputs clear only for an actual changed flow; `pageshow`, sorting/filtering and grid refreshes preserve entered values.
- Verified New Invoice hides fully allocated rows per selected flow, keeps the same billing record available in other flows, reports eligible row counts, clears and disables hidden inputs, and shows the flow-specific empty state. The Allocate column provides a 110px minimum input within a 140px desktop column and retains mobile table scrolling.
- Verified one receipt can cover several invoices for the same firm/customer, while mixed-customer and mixed-firm receipts and overpayments are rejected. Invoice and receipt cancellation recalculate the derived states.
- Verified concurrent worker payments, invoice allocations and receipt allocations preserve their caps. PostgreSQL serialization/deadlock exceptions, including wrapped `40001`, roll back and return the normal refresh/retry business error without automatic retry.
- Verified legacy invoice/date and receipt data migrates into Invoice, InvoiceLine and CustomerReceiptAllocation rows without changing the historical amount or removing audit/cancellation data.
- Verified login redirects across application pages, CSRF rejection, safe login return URLs, Admin access, failed-login lockout and disabled-session revocation.
- Verified AccountingFirm A cannot read AccountingFirm B data, Manager A cannot read Manager B data, and Worker A cannot read or mutate Worker B assignments or payments, including manually substituted URL/form IDs.
- Verified workers can submit only reports for their own active assignments, can submit missing past weeks, can edit a current-week report with the matching version, and cannot rewrite a submitted past report, submit a future week, duplicate an assignment/week, report for a cancelled assignment, or use another worker's URL/form ID. Admin/InternalUser can review reports, Manager sees only reports under the manager's engagements without worker financial fields, AccountingFirm is denied, and workers cannot POST shared WorkItem status/notes.
- Verified dashboard totals, lists, reports and CSV exports use the same entity scope. External detail pages and exports omit other-party shares, LCM gross/retained amounts, worker costs and unrelated receipt/payment data according to role.
- Verified Admin retains full access, InternalUser retains LCM operational access without user administration/cancellation, invalid role/link combinations are rejected, and changing a role or entity link revokes the existing session.
- Browser walkthrough: generated sample September billing; created the customer invoice document; allocated a RM250 customer receipt; assigned a worker at 70% of LCM's share; recorded a RM100 worker payment. Dashboard showed RM1,000 billing, RM350 firm, RM250 manager, RM400 LCM gross, RM280 worker entitlement, RM120 retained, RM750 customer outstanding and RM180 worker outstanding.
- Browser checks: text search with Enter and empty results, year/month/day date filter display and month search, inclusive amount ranges with reversed bounds, Cancel preserving filters, Clear filters, column visibility and multiple-column sort indicators. No browser console errors were captured.
- Responsive register checked at 390×844: page width remained 390px; wide table content scrolled within its 360px container. Desktop layout was also inspected.
- PostgreSQL custom-format backup restored successfully into a separate `billing_restore_test` database. Restored user count (1), billing (RM1,000), worker entitlement (RM280) and payments (RM100) matched the source demo.
- JavaScript syntax check and CI YAML parsing passed. Docker is unavailable on this Windows PC; the production image and Compose smoke test passed in GitHub Actions for this commit in `master` run [34331334752](https://github.com/ChunMeng0127/BILLING_MANAGEMENT/actions/runs/34331334752) and `codex/accounting-mvp` run [34331334588](https://github.com/ChunMeng0127/BILLING_MANAGEMENT/actions/runs/34331334588).

## Historical deployment verification (earlier release; Part 4.6, Part 4.7 and Part 5 were not deployed to Hostinger)

Docker is not installed on this Windows PC, so the local Docker image and Nginx/TLS stack were not executed here. GitHub Actions passed the PostgreSQL suite, Docker image build and production HTTPS Compose smoke test for the earlier release. Hostinger MCP project update action **113465100** completed successfully for `billing-control` on VM `1910481`: the builder and `InvoiceIssuerAndReceiptIntegrity` migration containers exited 0, the application was running, and PostgreSQL was healthy. `https://billing.lcmmgt.com/Account/Login` returned 200 with valid TLS; the protected `/Invoices` route returned the expected unauthenticated redirect. This is historical deployment evidence for the earlier release; the current Part 4.6, Part 4.7 and Part 5 changes remain undeployed to Hostinger.

The local database contains explicitly marked demonstration records. Credentials, database binaries/data, backups, key material and build outputs are excluded from Git. Part 5 has not been deployed to Hostinger; this validation documents the local and CI candidate only.
